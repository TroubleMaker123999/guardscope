fastapi>=0.110,<0.116
uvicorn[standard]>=0.27
typer>=0.12
sqlalchemy>=2.0
pydantic>=2.6
jinja2>=3.1
# Starlette's TestClient (used by FastAPI) recommends httpx < 0.28 in the
# version we tested against. Pin to that range so CI doesn't pull a newer
# httpx that drops the deprecated `app`/``client`` coercion we rely on.
httpx>=0.27,<0.28
# FastAPI 0.115+ imports python-multipart unconditionally, even for routes
# that never accept form data. The tests trigger that import path through
# the CorsMiddleware preflight check; install it explicitly so CI doesn't
# fail on a "Form data requires python-multipart" RuntimeError.
python-multipart>=0.0.9
pytest>=8.0